/**
 * The field and label styles every 64 Studios form uses, so the enquiry form
 * and the checklist sign-up cannot drift apart visually. A hairline bone
 * rule under each field that darkens to ink on focus, labels always visible
 * above the field, never a placeholder standing in for one.
 */
export const FIELD =
  "mt-1 w-full rounded-none border-0 border-b border-bone bg-transparent py-1 font-body text-base text-ink transition-colors duration-400 focus:border-ink";

export const LABEL = "font-body text-sm text-ink";
